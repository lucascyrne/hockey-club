# Frontend (Vercel) — URL do servidor WebSocket
# Dev: omitir para usar proxy Vite → localhost:8787
# VITE_WS_URL=wss://hockey-table-ws.fly.dev/ws

# Servidor (Fly/Railway/local)
# PORT=8787
# ALLOWED_ORIGINS=https://your-app.vercel.app,http://localhost:5173
# Dev: origens LAN (192.168.*, 10.*) aceites automaticamente
# ALLOWED_ORIGINS=*  # só se necessário em produção
